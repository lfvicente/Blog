<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h2>Comentarios</h2>

<%= render @article.comments %>
	
<h2>añadir comentario:</h2>

<%= render "comments/form" %>

<%= link_to 'Back', articles_path %>
<%= link_to 'Edit', articles_path(@article) %>

<%= link_to('Reader', 
		article_path(@article, :title => "Reader" ), 
		{ :method => :put }) %>


<%= link_to('Writer', 
		article_path(@article, :title => "Writer" ), 
		{ :method => :put }) %>


<%= debug(params) %>